#include "UsedList.hpp"

template <class Key, class T> bool UsedList<Key, T>::Exists(const T &entry) const
{
}

// Returns true if inserted, false if entry already exists
template <class Key, class T> bool UsedList<Key, T>::Insert(T entry)
{
}
